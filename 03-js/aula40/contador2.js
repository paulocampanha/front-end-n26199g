// Nesse programa vamos estudar o uso da estrura de repetição do while
// Nessa estrutura o teste lógico acontece após a 1 primeira execução
// do bloco de código dentro da estrutura.

let contador = 0;

do {
    console.log(contador);
    contador++;
} while (contador <= 10);
console.log("=".repeat(40));