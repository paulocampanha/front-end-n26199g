// Nesse programa vamos imprimir os itens de uma Array de futas usando a 
// estrutura for

const frutas = ["Maçã", "Banana", "Pera", "Uva", "Morango"];
for (let i = 0; i < frutas.length; i++) {
    console.log("Eu gosto de " + frutas[i]);
}
